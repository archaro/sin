/* Itemstore persistence keeps the application's configuration as an external
 * global.  Framework binaries do not start an application, so provide the
 * intentionally inert definition here. */
#include "config.h"

CONFIG_t config;
